import React from 'react';

import {CollectionContainer} from './Collection.style'

interface CollectionProps {}

export const Collection = (props: CollectionProps) => {
  return <CollectionContainer>Collection</CollectionContainer>;
};