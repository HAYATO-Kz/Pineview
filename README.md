# Requirement
OS: macOS

Xcode application with iOS Simulator

# Setup

Install Node
`brew install node`

Install Watchman
`brew install watchman`

Install node
`sudo gem install cocoapods`

# Installation

1. yarn